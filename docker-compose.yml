services:
  db:
    image: postgres:15-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=pedidos11
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    restart: always

  backend:
    build: ./backend
    command: /entrypoint.sh
    volumes:
      - ./backend:/app
      - static_volume:/app/staticfiles
      - media_volume:/app/media
    ports:
      - "8002:8000"
    environment:
      - DATABASE=postgres
      - SQL_ENGINE=django.db.backends.postgresql
      - SQL_DATABASE=pedidos11
      - SQL_USER=postgres
      - SQL_PASSWORD=postgres
      - SQL_HOST=db
      - SQL_PORT=5432
      - ALLOWED_HOSTS=*
      - DEBUG=0
      - CORS_ALLOW_ALL=True
      - CORS_ALLOWED_ORIGINS=http://localhost:3002,http://127.0.0.1:3002,http://192.168.1.121:3002
      - CSRF_TRUSTED_ORIGINS=http://localhost:3002,http://127.0.0.1:3002,http://192.168.1.121:3002
    depends_on:
      - db
    restart: always

  frontend:
    build:
      context: ./frontend
      args:
        - NEXT_PUBLIC_API_URL=/api
    ports:
      - "3002:3000"
    environment:
      - NODE_ENV=production
      - API_URL_INTERNAL=http://backend:8000/api
    depends_on:
      - backend
    restart: always

volumes:
  postgres_data:
  static_volume:
  media_volume:
